# Web / VPS / Docker (Flask + gunicorn) — no Kivy/desktop stack.
# Use this on Contabo and similar servers: pip install -r requirements-web.txt
Flask==2.3.3
Flask-CORS==4.0.0
requests==2.31.0
python-dotenv==1.0.0
polyline==2.0.3
mercantile==1.2.1
geopy==2.4.0
protobuf==5.28.2
boto3==1.35.24
gunicorn==21.2.0
Flask-Limiter==3.5.0
redis==5.0.1
PyJWT==2.10.1
cryptography
stripe>=10.0.0
