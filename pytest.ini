[pytest]
# Tests live in tests/ but import top-level app modules (e.g. `from speed_limit_detector
# import ...`). pythonpath = . puts the repo root on sys.path so those imports resolve
# regardless of where pytest is invoked from. (Requires pytest >= 7.)
pythonpath = .
testpaths = tests
