name: Deploy to Railway

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Notify Railway Deployment
        run: |
          echo "✅ Code pushed to GitHub!"
          echo "Railway will automatically deploy this to production."
          echo "Your app is live at: https://voyagr-production.up.railway.app"
          echo ""
          echo "Note: Railway automatically deploys when you push to GitHub."
          echo "No manual deployment action needed."

